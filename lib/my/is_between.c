/*
** EPITECH PROJECT, 2021
** Pool day 6
** File description:
** is between (not a task)
*/

int is_btwn(char c1, char c2, char c3)
{
    if ((c1 <= c3) && (c1 >= c2))
        return (1);
    return (0);
}
